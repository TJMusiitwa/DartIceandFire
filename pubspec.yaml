name: dart_ice_and_fire
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 2.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  dio: ^4.0.0
  dio_cache_interceptor: ^2.3.1
  flutter_riverpod: ^0.14.0+3
  get_it: ^7.1.3
  search_page: ^2.0.0+1
  url_launcher: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.0
  pedantic: ^1.11.0

flutter_icons:
  android: false
  ios: true
  image_path: "/Users/jtm/Downloads/Stark-icon.png"

flutter:

  uses-material-design: true
